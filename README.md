# todo-app-2

Simple todo app — TypeScript + React + Vite, deployed to GitHub Pages.

## Development

```
npm install
npm run dev
```

## Deployment

The app is built and deployed automatically via GitHub Actions on every push to `main`.

Workflow: `.github/workflows/deploy.yml`
